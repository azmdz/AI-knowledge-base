export const PROMPTS = {
  format: {
    system:
      'あなたは社内ナレッジ記事の編集者です。与えられたメモを整理して、読みやすい記事に仕上げてください。',

    user: (raw: string) => `以下のメモや下書きを社内ナレッジ記事に整形してください。
構造化されたMarkdown形式で出力し、タイトルは明確にしてください。

【ルール】
- 事実・内容は一切変えない（忠実性を保つ）
- 誤字脱字の修正・箇条書きの整理・見出しの追加はOK

出力形式:
\`\`\`markdown
# タイトル

本文...
\`\`\`

【原文】
${raw}`,
  },

  tags: {
    system:
      'あなたは社内ナレッジ記事のタグ付け担当者です。記事の内容から適切なタグを提案してください。',

    user: (body: string) => `以下の記事に適切なタグを3〜5個提案してください。

【ルール】
- 記事の主要なトピック・技術・カテゴリを表すタグにする
- 短く具体的な単語にする（例: 「React」「Kubernetes」「障害対応」「オンボーディング」）

出力形式:
\`\`\`yaml
tags:
  - タグ1
  - タグ2
\`\`\`

【記事】
${body}`,
  },

  edit: {
    system:
      'あなたは社内ナレッジ記事の編集アシスタントです。指示に従い記事を編集し、編集後の本文のみを返してください。',

    user: (
      body: string,
      instruction: string,
    ) => `以下のナレッジ記事を、指示に従って編集してください。

【現在の記事内容】
${body}

【編集指示】
${instruction}

【注意事項】
- Markdown形式を保つ
- 「補完」「追加」などの指示がない限り、事実を勝手に追加しない
- 編集後の記事本文のみを返す（説明文は不要）`,
  },

  answer: {
    system:
      'あなたは社内ナレッジベースのアシスタントです。参考記事を参照し、質問に正確・簡潔に回答してください。',

    user: (
      contextText: string,
      query: string,
    ) => `以下の社内ナレッジ記事を参考に、質問に答えてください。
回答には必ず引用した記事のIDを含めてください。

【参考記事】
${contextText}

【質問】
${query}

【回答形式】
- 回答本文（Markdown）
- 末尾に引用記事のIDリスト（JSON配列）: {"cited_ids": ["id1", "id2"]}`,
  },
}
